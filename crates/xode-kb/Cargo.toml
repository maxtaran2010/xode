[package]
name = "xode-kb"
version.workspace = true
edition.workspace = true

[dependencies]
xode-core.workspace = true
xode-db = { path = "../xode-db" }
tokio.workspace = true
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
parking_lot.workspace = true
tracing.workspace = true
regex.workspace = true
once_cell.workspace = true
sha2.workspace = true
hex.workspace = true
reqwest = { workspace = true, features = ["blocking"] }
ignore = "0.4"
notify-debouncer-mini = "0.7"
fastembed = { version = "7", default-features = false, features = ["ort-download-binaries-rustls-tls", "hf-hub-rustls-tls"], optional = true }

[features]
default = ["builtin-embed"]
# Built-in ONNX embeddings via fastembed. Off -> gateway embeddings only (no ONNX Runtime dep).
builtin-embed = ["dep:fastembed"]

[dev-dependencies]
tempfile = "3"
tokio-util.workspace = true
